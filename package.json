{
  "name": "zotero-margin",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "description": "Margin — an AI reading companion inside Zotero",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/*.test.ts",
    "check": "npm run typecheck && npm test && npm run build",
    "preview": "node scripts/preview.mjs"
  },
  "dependencies": {
    "katex": "^0.16.22",
    "markdown-it": "^14.1.0"
  },
  "devDependencies": {
    "@types/katex": "^0.16.7",
    "@types/markdown-it": "^14.1.2",
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.0",
    "fflate": "^0.8.2",
    "jsdom": "^26.1.0",
    "@types/jsdom": "^21.1.7",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  }
}
